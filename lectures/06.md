---
title: "Найближчі сусіди, дерева рішень та ансамблі"
outline: [2, 2]
---

# Лекція 06 · Найближчі сусіди, дерева рішень та ансамблі

У попередній лекції межу між класами задавало рівняння прямої. Сьогодні розглянемо методи, які рівняння не пишуть узагалі: одні запам'ятовують дані й вирішують за схожістю, інші розрізають простір ознак послідовністю простих питань. Наприкінці об'єднаємо сотні таких моделей в ансамбль і побачимо, чому саме він виграє на табличних даних.

## Зміст лекції

Шлях лекції — від найпростішої ідеї «подивись на сусідів» до ансамблів, які сьогодні є стандартним вибором для табличних даних.

1. **Метод k найближчих сусідів.** Прогноз за схожістю, вибір k, роль масштабу ознак.
2. **Прокляття розмірності.** Чому в багатовимірному просторі слово «близько» втрачає зміст.
3. **Дерево рішень.** Вкладені перевірки, критерії розбиття, читання правил на конкретному пасажирі.
4. **Перенавчання й обрізання дерева.** Глибина, кількість листків, обрізання за складністю.
5. **Беггінг і випадковий ліс.** Бутстреп, голосування, зниження дисперсії, важливість ознак.
6. **Градієнтний бустинг.** Послідовне навчання на залишках і його регуляризація.

Кожен блок закриває слабке місце попереднього: сусіди страждають від розмірності — дерево розглядає ознаки по одній; одне дерево нестабільне — ліс усереднює багато дерев; ліс не вміє цілеспрямовано виправляти власні помилки — бустинг будує дерева одне за одним саме заради цього.

## Чого ви навчитеся

Після цієї лекції ви зможете:

- пояснити, як метод k найближчих сусідів робить прогноз, і чому в нього немає фази навчання;
- обґрунтувати вибір k через компроміс зміщення й дисперсії та показати його на кривій похибки;
- назвати два наслідки прокляття розмірності й оцінити, коли метод сусідів перестає працювати;
- побудувати дерево рішень, прочитати його правила вголос і простежити шлях конкретного об'єкта до листа;
- порахувати ентропію вузла та приріст інформації для кількох правил і обрати найкраще;
- розпізнати перенавчене дерево й зменшити його обрізанням за складністю;
- пояснити, чому беггінг знижує дисперсію, а бустинг — зміщення, і коли який доречніший;
- прочитати важливість ознак за зменшенням домішки та пермутаційну важливість і сказати, коли перша обманює.

## Дві родини методів без рівняння межі

Логістична регресія з лекції 05 описувала **межу рішення** (*decision boundary*) одним рівнянням: знайшли вектор ваг — і модель готова, скільки б не було даних. Такі моделі називають **параметричними** (*parametric*).

Сьогоднішні методи влаштовані інакше. Метод найближчих сусідів моделі не будує взагалі: він зберігає всю таблицю й для кожного нового об'єкта дивиться, на кого той схожий. Дерево рішень модель будує, але не у вигляді формули, а у вигляді послідовності простих перевірок «ознака більша за поріг чи ні».

> *"Non-parametric models: number of parameters grows with n; size of model depends on n."*
> — UBC CPSC 340, лекція 6

Обидві родини легко описують нелінійні межі й не потребують, щоб класи розділялися прямою. Плату за цю свободу побачимо далі.

## Дані лекції: вина, пасажири «Титаніка» і пухлини

Три набори, кожен показує свій бік теми.

**Wine** — 178 зразків вина трьох сортів, 13 хімічних вимірів: вміст спирту, флаваноїдів, магнію, проліну, інтенсивність кольору. Передбачаємо сорт. Ознаки виміряні в різних одиницях, тому набір добре показує, що робить масштабування з методом сусідів.

**Titanic** — 891 пасажир, ознаки змішані: числові (вік, вартість квитка), категоріальні (стать, порт посадки) і з пропусками (вік невідомий у 177 випадках). Передбачаємо, чи вижив пасажир. Саме тут дерево показує свою сильну сторону.

**Breast Cancer Wisconsin** — 569 пухлин, 30 числових ознак; на ньому наприкінці порівняємо всі методи лекції між собою.


<Figure src="/figs/own/lec06_06_01_wine_data.png" alt="wine dataset class balance and two feature scatter">
</Figure>


Джерела даних: [Wine, UCI](https://archive.ics.uci.edu/dataset/109/wine) · [Titanic, seaborn-data](https://github.com/mwaskom/seaborn-data) · [Breast Cancer, UCI](https://archive.ics.uci.edu/dataset/17/breast+cancer+wisconsin+diagnostic)

## Метод k найближчих сусідів

Ідея вкладається в одне речення: щоб класифікувати новий об'єкт, знайди в тренувальній вибірці k найсхожіших на нього і віддай найпоширенішу серед них мітку.

> *"To classify an example: 1. Find the 'k' training examples that are 'nearest' to it. 2. Classify using the most common label of 'nearest' training examples."*
> — UBC CPSC 340, лекція 6

Метод спирається на одне припущення: об'єкти зі схожими ознаками мають схожі мітки. Фази навчання тут немає взагалі — алгоритм просто зберігає таблицю, тому його називають **лінивим навчанням** (*lazy learning*): уся робота відкладена на момент запиту.

На рисунку одна й та сама точка, позначена хрестиком, при k = 1 отримує мітку «сорт 2», бо саме такий її найближчий сусід, а при k = 15 — «сорт 1», бо в ширшому колі переважають сині зразки. Прогноз залежить від k не менше, ніж від самих даних.


<Figure src="/figs/own/lec06_06_02_knn_idea.png" alt="same query point classified differently for k equal 1 and 15">
</Figure>

## Формула прогнозу і міра близькості

Позначимо тренувальну вибірку $S=\{(\mathbf{x}_n, y_n)\}_{n=1}^{N}$, а через $\mathrm{nbh}_{S,k}(\mathbf{x})$ — множину k найближчих до $\mathbf{x}$ точок цієї вибірки. Прогноз залежить від задачі: у регресії це середнє міток сусідів, у класифікації — голос більшості, тобто клас із найбільшою кількістю голосів.

$$f^{\text{рег}}_{S,k}(\mathbf{x}) = \frac{1}{k}\!\!\sum_{\mathbf{x}_n \in \mathrm{nbh}_{S,k}(\mathbf{x})}\!\! y_n, \qquad f^{\text{клас}}_{S,k}(\mathbf{x}) = \arg\max_{c}\!\!\sum_{\mathbf{x}_n \in \mathrm{nbh}_{S,k}(\mathbf{x})}\!\! \mathbf{1}[y_n = c]$$

$$\mathrm{dist}(\mathbf{x}_n, \tilde{\mathbf{x}}) = \sqrt{\sum_{d=1}^{D}\bigl(x_{nd} - \tilde{x}_d\bigr)^2}$$

| Позначення | Значення |
|---|---|
| $N$, $D$ | кількість зразків і кількість ознак |
| $k$ | скільки сусідів беремо до уваги |
| $\mathrm{nbh}_{S,k}(\mathbf{x})$ | самі сусіди: k найближчих точок вибірки |
| $\mathrm{dist}$ | евклідова відстань; коштує $O(D)$ на пару точок |

Дві дрібниці, які видно лише на практиці. Множина сусідів визначена неоднозначно, коли кілька точок лежать на однаковій відстані; за парного k можлива нічия в голосуванні, тому k беруть непарним. Замість простого голосу сусідам часто дають ваги, обернені до відстані — ближчий сусід важить більше.

## Мале k дає рвану межу, велике — гладку

Параметр k керує складністю моделі, причому в незвичний бік: чим менше k, тим складніша модель.

При k = 1 межа рішення проходить точно між точками різних класів, утворюючи острівці навколо окремих зразків. Тренувальна похибка при цьому дорівнює нулю: найближчий сусід кожної тренувальної точки — вона сама. (Виняток — коли два зразки мають однакові ознаки, але різні мітки; тоді нуля не буде.) Це класичне **перенавчання** (*overfitting*): низьке зміщення, висока дисперсія.

При k = 5 окремі викиди вже не мають вирішального голосу, і межа стає гладкішою. У протилежній крайності, коли k дорівнює розміру вибірки, модель усім об'єктам ставить найпоширенішу мітку і не залежить від входу взагалі.


<Figure src="/figs/ext/cs231n.github.io-b1f49a-knn.jpeg" alt="the data, one nearest neighbour and five nearest neighbours decision boundaries">

Джерело: [Stanford CS231n · Image Classification](https://cs231n.github.io/classification/)

</Figure>

## Три значення k на даних Wine

Те саме на реальних даних: три сорти вина у двох ознаках, модель навчено на 124 зразках, перевірено на 54.

При k = 1 регіони рвані, всередині синьої зони видно помаранчеві острівці навколо окремих зразків. При k = 15 межа згладжується, і точність на тесті навіть зростає — від 0,93 до 0,94. При k = 100 сусідами стають майже всі тренувальні зразки, регіон одного класу поглинає решту, і точність падає до 0,39, тобто до рівня найбільшого класу.


<Figure src="/figs/own/lec06_06_03_knn_k_effect.png" alt="knn decision regions on wine for k equal 1, 15 and 100">
</Figure>


Головний висновок практичний: k підбирають, а не вгадують, і робити це треба на окремій вибірці, а не на тій, якою модель уже володіє.

<KnnLab />

## Вибір k як компроміс зміщення й дисперсії

Побудуємо криву похибки для всіх k від 1 до 60. Оцінюємо десятиразовою перехресною перевіркою з десятьма повтореннями, щоб крива не тремтіла від випадковості розбиття.

При k = 1 тренувальна похибка дорівнює нулю, а на нових даних модель помиляється в 4,4 % випадків. Оптимум припадає на k = 34 з похибкою 2,0 %. Далі похибка знову зростає: до k = 60 вона повертається на рівень 4,1 %, бо модель стає надто грубою.


<Figure src="/figs/own/lec06_06_04_knn_bias_variance.png" alt="training and cross validation error versus k on wine">
</Figure>


Зверніть увагу на характерну для сусідів деталь: за винятком k = 1 тренувальна й перевірочна криві майже збігаються. Метод нічого не «підганяє» під тренувальні дані — він їх просто зберігає, тому розрив між двома кривими тут значно менший, ніж у моделей з параметрами.

## Масштаб ознак вирішує, хто вважається сусідом

Евклідова відстань додає квадрати різниць по всіх ознаках. Якщо одна ознака виміряна в сотнях, а решта — в одиницях, вона одна визначить суму, і сусідами стануть об'єкти, схожі лише за нею.

У наборі Wine вміст проліну лежить у межах від 278 до 1680, а вміст флаваноїдів — від 0,34 до 5,08. Розмах відрізняється в триста разів, тому на сирих даних відстань фактично рахується по одному проліну.


<Figure src="/figs/own/lec06_06_05_knn_scaling.png" alt="feature ranges and knn accuracy before and after scaling">
</Figure>


Наслідок вимірюється прямо: на сирих ознаках точність десятиразової перехресної перевірки становить 0,670, після стандартизації — 0,966. Це той самий алгоритм і ті самі дані; змінилися тільки одиниці виміру. Для методів, що спираються на відстань, масштабування ознак не косметика, а обов'язковий крок.

## Код: метод сусідів на даних Wine

Стандартизацію обов'язково загортаємо в конвеєр: інакше середнє й стандартне відхилення порахуються по всій вибірці разом із тестовою частиною, і в модель просочиться інформація про тест.

```python
from sklearn.datasets import load_wine
from sklearn.model_selection import cross_val_score, train_test_split
from sklearn.neighbors import KNeighborsClassifier
from sklearn.pipeline import make_pipeline
from sklearn.preprocessing import StandardScaler

wine = load_wine()                                  # 178 зразків, 13 ознак
X_tr, X_te, y_tr, y_te = train_test_split(
    wine.data, wine.target, test_size=0.3, stratify=wine.target, random_state=0)

knn = make_pipeline(StandardScaler(), KNeighborsClassifier(n_neighbors=7))
knn.fit(X_tr, y_tr)                                 # «навчання» = запам'ятати дані

print("зі стандартизацією:", round(knn.score(X_te, y_te), 3))
print("без стандартизації:",
      round(KNeighborsClassifier(7).fit(X_tr, y_tr).score(X_te, y_te), 3))
print("перехресна перевірка:",
      round(cross_val_score(knn, wine.data, wine.target, cv=10).mean(), 3))
```

<RunOutput>

```text
зі стандартизацією: 0.963
без стандартизації: 0.685
перехресна перевірка: 0.966
```

</RunOutput>


Вивід: `зі стандартизацією: 0.963`, `без стандартизації: 0.685`, `перехресна перевірка: 0.966`.

Джерело: [scikit-learn · Nearest Neighbors](https://scikit-learn.org/stable/modules/neighbors.html)

## Прокляття розмірності: простір швидко порожніє

У методу сусідів є ворог, який не видно на двовимірних картинках. Візьмімо дані, рівномірно розсипані в одиничному кубі розмірності $D$, і спитаймо, який завбільшки має бути маленький куб зі стороною $r$ навколо точки, щоб охопити частку $\alpha$ всіх даних.

$$\mathbb{P}(\mathbf{x} \in \text{куб}) = r^{D} = \alpha \;\Longrightarrow\; r = \alpha^{1/D}$$

Щоб охопити один відсоток даних, при $D = 10$ потрібна сторона 0,63, а при $D = 100$ — уже 0,95. Тобто «локальна» околиця займає майже весь простір і локальною бути перестає.

Другий бік тієї самої монети: для $N$ точок радіус, у якому з імовірністю не менше ½ знайдеться хоч один сусід, дорівнює $r \ge \bigl(1 - (1/2)^{1/N}\bigr)^{1/D}$. Для $D = 10$ і $N = 500$ це 0,52 — половина сторони куба.


<Figure src="/figs/own/lec06_06_06_curse_geometry.png" alt="cube side and neighbour radius grow with dimension">
</Figure>

## Прокляття розмірності на реальних даних

Формули легко перевірити експериментом. До тринадцяти корисних ознак Wine додамо ознаки з чистого шуму — від п'яти до двохсот — і подивимось, як поводяться три різні моделі.

Метод сусідів просідає найсильніше: з 0,966 до 0,704 — шумові координати входять у відстань нарівні з корисними. Логістична регресія тримається краще, бо дає шумовим ознакам малі ваги. Випадковий ліс майже не помічає шуму: на кожному розбитті він обирає одну ознаку і просто ніколи не обирає безкорисну.


<Figure src="/figs/own/lec06_06_07_curse_on_wine.png" alt="accuracy drop with added noise features for knn forest logistic">
</Figure>


> *"As the dimensionality grows, fixed-size training sets cover a diminishing fraction of the input space."*
> — EPFL CS-433, лекція 06b

Теорія каже те саме, але формулювати її треба точно. **Асимптотично** похибка одного сусіда не перевищує подвоєної похибки найкращого класифікатора. Для скінченної вибірки додається доданок порядку $\sqrt{D}\,N^{-1/(D+1)}$ — за умови ліпшицевості; саме з нього й виходить оцінка $D^{(D+1)/2}$, експоненційна за розмірністю.

Висновок: перед kNN ознаки варто відібрати або стиснути — цим займемось у лекції 09.

## Дерево рішень: програма з вкладених перевірок

Перейдемо до другої родини. Дерево рішень не рахує відстаней узагалі: воно ставить об'єкту послідовність простих питань про окремі ознаки.

> *"Decision trees are simple programs consisting of: a nested sequence of 'if-else' decisions based on the features (splitting rules); a class label as a return value at the end of each sequence."*
> — UBC CPSC 340, лекція 3

Найпростіше дерево з одним правилом називають **пеньком рішення** (*decision stump*). Повне дерево будують жадібно: навчають пеньок, ділять дані на дві частини й повторюють процедуру в кожній із них, доки не спрацює критерій зупинки.

У кожному вузлі рисунка написано те саме: правило розбиття, домішка вузла, скільки зразків до нього дійшло і як вони розподілені між класами. У листках правила вже немає — там лише прогноз.


<Figure src="/figs/ext/scikit-learn.org-ca74af-sphx_glr_plot_unveil_tree_structure_001.png" alt="anatomy of decision tree nodes with rule impurity and class counts">

Джерело: [scikit-learn · Understanding the decision tree structure](https://scikit-learn.org/stable/auto_examples/tree/plot_unveil_tree_structure.html)

</Figure>

<Materials :items='[
    { title: "scikit-learn · Decision Trees", url: "https://scikit-learn.org/stable/modules/tree.html", kind: "документація", note: "Критерії розбиття, обрізання та всі гіперпараметри з поясненням." },
    { title: "MLU-Explain · Decision Trees", url: "https://mlu-explain.github.io/decision-tree/", kind: "демо", note: "Інтерактивне пояснення від Amazon: дерево росте на очах, кожне розбиття видно на даних." }
]' />

## Дані «Титаніка»: пропуски, категорії й дисбаланс

Дерево показуємо на даних, які виглядають як типова робоча таблиця, а не як чистий навчальний приклад.

З 891 пасажира вижили 342, тобто 38 %. Вік невідомий для 177 осіб, номер палуби — для 688; стать і порт посадки записані текстом. Метод сусідів на таких даних без серйозної підготовки не запустиш: пропуски треба заповнити, категорії закодувати, ознаки масштабувати.

Дереву потрібне тільки кодування категорій. Пропуски у віці заповнимо медіаною — 28 років — і скажемо про це вголос: заповнення медіаною занижує розкид віку й трохи згладжує роль цієї ознаки.


<Figure src="/figs/own/lec06_06_08_titanic_data.png" alt="titanic missing values and survival rate by sex and class">
</Figure>


Права панель уже підказує, що знайде дерево: серед жінок першого класу вижили 97 %, серед чоловіків третього — 14 %. Стать і клас квитка виявляться двома найпершими питаннями.

Джерело даних: [seaborn-data · titanic.csv](https://github.com/mwaskom/seaborn-data)

## Дерево глибини 3 на даних «Титаніка»

Навчимо дерево з обмеженням глибини трьома рівнями на 623 пасажирах, а 268 залишимо для перевірки. Точність на відкладеній вибірці — 0,832 проти 0,616 у примітивного правила «всі загинули».


<Figure src="/figs/own/lec06_06_09_titanic_tree.png" alt="decision tree of depth three trained on titanic data">
</Figure>


Правила читаються вголос майже літературно. Перше питання — стать. Жінки з першого та другого класу вижили майже всі: у відповідному листку 121 пасажирка, з них вижили 115. Чоловіки старші за 13 років гинули майже поголовно: найбільший листок дерева зібрав 275 таких пасажирів із дешевшими квитками, і вижили з них 26.

Саме ця прозорість — головна перевага дерева. Модель не просто видає число: вона видає правило, яке лікар, слідчий або кредитний комітет можуть прочитати й оскаржити.

## Шлях одного пасажира по вузлах дерева

Прогноз для конкретного об'єкта — це шлях від кореня до листка. Візьмімо хлопчика двох років, який плив третім класом із трьома братами й сестрами, квиток коштував 21,1 £.


<Figure src="/figs/own/lec06_06_10_titanic_path.png" alt="path of one passenger through the nodes of the decision tree">
</Figure>


```python
boy = X[(tt.sex == "male") & (tt.age < 8) & (tt.pclass == 3)].iloc[[0]]
nodes = tree.decision_path(boy).indices          # усі вузли шляху

for nid in nodes[:-1]:
    j = tree.tree_.feature[nid]
    val, thr = boy.iat[0, j], tree.tree_.threshold[nid]
    print(f"вузол {nid}: {X.columns[j]} = {val:g}  "
          f"{'<=' if val <= thr else '>'} {thr:.1f}")
print("лист", nodes[-1], "→ P(вижив) =", tree.predict_proba(boy)[0, 1].round(3))
```

<RunOutput>

```text
вузол 0: sex = 1  > 0.5
вузол 8: age = 2  <= 13.0
вузол 9: sibsp = 3  > 2.5
лист 11 → P(вижив) = 0.083
```

</RunOutput>


Вивід: `вузол 0: sex = 1 > 0.5` → `вузол 8: age = 2 <= 13.0` → `вузол 9: sibsp = 3 > 2.5` → `лист 11 → P(вижив) = 0.083`. Хлопчик молодший за поріг у 13 років, тобто потрапив у «дитячу» гілку, але велика родина в третьому класі перекреслила цю перевагу: у листку 12 пасажирів, вижив один. Пасажир справді загинув.

## Пошук найкращого розбиття: домішка й приріст інформації

Дерево обирає розбиття перебором: для кожної ознаки й кожного порога рахує, наскільки чистішими стали нащадки. Чистоту міряють **домішкою вузла** (*impurity*) — ентропією або індексом Джині:

$$H(S) = -\sum_{c=1}^{K} p_c \log_2 p_c, \qquad \mathrm{Gini}(S) = 1 - \sum_{c=1}^{K} p_c^2, \qquad IG = H(S) - \sum_{b \in \{L, R\}} \frac{|S_b|}{|S|}\,H(S_b)$$

Тут $p_c$ — частка класу $c$ у вузлі, $S_L$ і $S_R$ — ліва й права частини після розбиття, а ваги $|S_b|/|S|$ не дають маленькій чистій гілці перетягнути рішення на себе. **Приріст інформації** (*information gain*) $IG$ — це те, наскільки впала середня домішка.


<Figure src="/figs/own/lec06_06_11_impurity_infogain.png" alt="impurity measures and information gain of candidate splits">
</Figure>


Обидві міри максимальні на суміші п'ятдесят на п'ятдесят і дорівнюють нулю на чистому вузлі, тому на практиці дають майже однакові розбиття. У scikit-learn за замовчуванням стоїть Джині — він трохи дешевший, бо не потребує логарифма. Означення подано за [ESL, розділ 9.2](https://hastie.su.domains/ElemStatLearn/), де описано алгоритм CART.

## Числовий приклад: перебір правил у корені

Порахуймо все руками на 623 пасажирах тренувальної частини. Вижили 239 із них, отже $p = 0{,}384$ і ентропія кореня $H = -0{,}384\log_2 0{,}384 - 0{,}616\log_2 0{,}616 = 0{,}961$ біта.

| Правило розбиття | Ентропія після, біт | Приріст $IG$, біт |
|---|---|---|
| стать = жінка | 0,747 | **0,214** |
| вартість квитка < 10 £ | 0,897 | 0,064 |
| клас квитка = 1 | 0,904 | 0,057 |
| порт посадки = Cherbourg | 0,947 | 0,014 |
| вік < 6,5 років | 0,954 | 0,007 |

Переможець — стать: він єдиний забирає понад одну п'яту всього безладу. Саме це правило й стоїть у корені дерева з попередніх карток.

Порівняння двох останніх рядків повчальне. Правило «вік < 6,5 років» ділить пасажирів дуже нерівно: маленьких дітей у вибірці всього кілька десятків, тому навіть якщо в цій гілці вижили майже всі, зважений внесок мізерний. Корисне розбиття має бути водночас чистим і достатньо великим.

## Дерево розрізає простір на прямокутники

Кожна перевірка виду «ознака $x_d$ менша за поріг» проводить у просторі ознак пряму, перпендикулярну до осі $x_d$. Тому межа рішення дерева завжди складається з відрізків, паралельних осям координат, — виходять сходинки, а не похилі лінії.


<Figure src="/figs/ext/scikit-learn.org-35bffa-sphx_glr_plot_iris_dtc_001.png" alt="axis parallel decision surface of decision trees on iris feature pairs">

Джерело: [scikit-learn · Decision surface of decision trees on iris](https://scikit-learn.org/stable/auto_examples/tree/plot_iris_dtc.html)

</Figure>


Звідси випливають дві протилежні властивості. З одного боку, дереву байдуже до масштабу ознак: поріг однаково добре знаходиться і в міліграмах, і в кілограмах, тому стандартизація йому не потрібна. З іншого боку, похилу межу дерево змушене апроксимувати сходинками, і на просто повернутих даних воно програє лінійній моделі.

## Дерево в задачі регресії

Той самий алгоритм працює і з числовою ціллю: замість голосу більшості в листку зберігається середнє значення, а замість ентропії мінімізується сума квадратів відхилень.


<Figure src="/figs/ext/scikit-learn.org-224c44-sphx_glr_plot_tree_regression_001.png" alt="decision tree regression fits of depth two and depth five">

Джерело: [scikit-learn · Decision Tree Regression](https://scikit-learn.org/stable/auto_examples/tree/plot_tree_regression.html)

</Figure>


Прогноз дерева — кусково-стала функція, і це видно неозброєним оком: між порогами лінія горизонтальна. Дерево глибини 2 дає грубу, але розумну сходинкову апроксимацію. Дерево глибини 5 уже ганяється за окремими точками: кожен викид отримує власну сходинку. Це і є перенавчання, тільки в регресійному вбранні.

Звідси й межа застосовності: дерево не екстраполює. За краєм тренувального діапазону воно назавжди повертає значення крайнього листка.

## Глибоке дерево запам'ятовує вибірку

Без обмежень дерево росте, доки кожен листок не стане чистим. На «Титаніку» це означає 143 листки й окремі правила для окремих пасажирів.


<Figure src="/figs/own/lec06_06_12_tree_depth.png" alt="train and holdout accuracy versus tree depth on titanic">
</Figure>


Криві розходяться характерним віялом. Точність на тренувальній вибірці монотонно зростає до 0,986 — дерево вивчило майже кожного пасажира напам'ять. Точність на відкладеній вибірці досягає максимуму 0,836 при глибині 5, а далі падає до 0,780, тобто гірше, ніж у дерева глибини 3.

Найпростіші засоби проти цього — критерії зупинки: максимальна глибина, мінімальна кількість зразків у вузлі, мінімальний приріст інформації. Усі вони діють наперед і тому грубі: зупинившись зарано, дерево не побачить корисного розбиття, яке відкрилося б рівнем нижче.

## Обрізання дерева за складністю

Акуратніший спосіб — виростити дерево повністю, а потім обрізати зайве. **Обрізання за складністю** (*cost-complexity pruning*) мінімізує суму «домішка листків плюс $\alpha$ помножити на кількість листків»: параметр $\alpha$ призначає ціну за кожен листок, і гілки, що не окупають цю ціну, відпадають.


<Figure src="/figs/ext/scikit-learn.org-3c470c-sphx_glr_plot_cost_complexity_pruning_003.png" alt="accuracy on train and test sets versus pruning parameter alpha">

Джерело: [scikit-learn · Post pruning decision trees with cost complexity pruning](https://scikit-learn.org/stable/auto_examples/tree/plot_cost_complexity_pruning.html)

</Figure>


```python
full = DecisionTreeClassifier(random_state=0).fit(X_tr, y_tr)
alphas = full.cost_complexity_pruning_path(X_tr, y_tr).ccp_alphas[:-1]
scores = [cross_val_score(DecisionTreeClassifier(ccp_alpha=a, random_state=0),
                          X_tr, y_tr, cv=5).mean() for a in alphas]
best = alphas[int(np.argmax(scores))]
pruned = DecisionTreeClassifier(ccp_alpha=best, random_state=0).fit(X_tr, y_tr)
```

Цей фрагмент нічого не друкує — він лише повертає обрізане дерево. На «Титаніку» найкраще $\alpha = 0{,}0038$: дерево скорочується зі 143 листків до 13, а точність на тесті зростає з 0,780 до 0,843.

<Materials :items='[
    { title: "scikit-learn · Post pruning decision trees with cost complexity pruning", url: "https://scikit-learn.org/stable/auto_examples/tree/plot_cost_complexity_pruning.html", kind: "документація", note: "Той самий шлях ccp_alpha, що в коді лекції, з поясненням кожного кроку." }
]' />

## Одне дерево нестабільне

У дерева є вада, яку не лікує ні обмеження глибини, ні обрізання: висока дисперсія. Оскільки розбиття обирається жадібно, зміна кількох зразків біля порога змінює правило в корені, а разом із ним і все дерево нижче.


<Figure src="/figs/own/lec06_06_13_tree_instability.png" alt="two trees trained on two bootstrap samples give different boundaries">
</Figure>


На рисунку два дерева навчено на двох випадкових вибірках з тих самих даних Wine. Алгоритм один, дані відрізняються лише перетасовкою з поверненням, а межі рішення вийшли помітно різними: точність 0,85 проти 0,91.

Нестабільність — погана новина для інтерпретації: правило, яке ми щойно читали вголос, могло б виглядати інакше на трохи інших даних. Але вона ж відкриває дорогу до головної ідеї другої половини лекції. Якщо модель сильно залежить від випадковості вибірки, її помилки теж випадкові — а випадкові помилки можна усереднити.

## Ансамбль: голосування виправляє незалежні помилки

Нехай є $m$ моделей, кожна відповідає правильно з імовірністю $p$, і їхні помилки незалежні. Голос більшості правильний з імовірністю, яку дає біноміальний розподіл.

Для трьох моделей із точністю 0,8 арифметика така: усі три праві з імовірністю $0{,}8^3 = 0{,}512$, рівно дві праві — $3 \cdot 0{,}8^2 \cdot 0{,}2 = 0{,}384$, разом **0,896**. Три моделі по 80 % дають майже 90 %.


<Figure src="/figs/own/lec06_06_14_ensemble_math.png" alt="majority vote accuracy and bootstrap unique fraction">
</Figure>


Ключове слово тут — незалежні. Ліва панель показує й зворотний бік: якщо кожна модель гірша за випадкове вгадування, голос більшості не рятує, а прискорює падіння (червона крива). А якщо всі моделі однакові, усереднення взагалі нічого не змінює. Тому моделям треба дати різні дані — **бутстреп-вибірку** (*bootstrap sample*) розміру $N$, набрану з поверненням. Кожна така вибірка містить у середньому $1 - (1 - 1/N)^N \approx 0{,}632$ унікальних зразків, а решта 36,8 % лишаються **поза мішком** (*out-of-bag*) і згодяться далі.

## Беггінг знижує дисперсію, не чіпаючи зміщення

**Беггінг** (*bagging*, від bootstrap aggregating) — це навчити багато однакових моделей на різних бутстреп-вибірках і усереднити їхні прогнози. Для $m$ моделей із дисперсією похибки $\sigma^2$ і попарною кореляцією $\rho$ дисперсія середнього дорівнює $\frac{\sigma^2}{m}\bigl[1 + (m-1)\rho\bigr]$: при $\rho = 0$ вона падає в $m$ разів, при $\rho = 1$ не падає взагалі.


<Figure src="/figs/ext/scikit-learn.org-89b9f6-sphx_glr_plot_bias_variance_001.png" alt="bias variance decomposition for a single tree and for bagged trees">

Джерело: [scikit-learn · Single estimator versus bagging](https://scikit-learn.org/stable/auto_examples/ensemble/plot_bias_variance.html)

</Figure>


На рисунку зверху — прогнози окремих дерев, навчених на різних вибірках: рожеві криві розкидані широко. Праворуч, після усереднення, розкид помітно менший, а середня крива майже не змінилася. Розклад похибки внизу пояснює це числами: зелена складова дисперсії просідає, синє зміщення лишається на місці.

Тому в беггінг беруть глибокі дерева: у них мале зміщення й велика дисперсія, і саме дисперсію усереднення вміє прибирати.

## Випадковий ліс додає випадковість у самі розбиття

Бутстрепу мало: дерева, навчені на перетасованих версіях тих самих даних, усе одно схожі, бо в корені майже завжди опиняється та сама найсильніша ознака. **Випадковий ліс** (*random forest*) додає другий механізм: у кожному вузлі розбиття шукається не серед усіх $D$ ознак, а серед випадкової підмножини. Дерева перестають повторювати одне одного, кореляція $\rho$ падає, і усереднення нарешті працює на повну силу.


<Figure src="/figs/ext/scikit-learn.org-bfe3d9-sphx_glr_plot_ensemble_oob_001.png" alt="out of bag error rate versus number of trees for three max features settings">

Джерело: [scikit-learn · OOB Errors for Random Forests](https://scikit-learn.org/stable/auto_examples/ensemble/plot_ensemble_oob.html)

</Figure>


Криві на рисунку відрізняються лише розміром цієї підмножини: варіант із $\sqrt{D}$ ознак дає нижчу похибку, ніж варіант із повним переліком, — саме тому, що обмеження вибору робить дерева несхожими. Вертикальна вісь — похибка на об'єктах, які не потрапили в бутстреп-вибірку конкретного дерева; таку оцінку отримують без окремої перевірочної вибірки.

Джерело методу: [Breiman L. Random Forests, 2001](https://link.springer.com/article/10.1023/A:1010933404324)

<Materials :items='[
    { title: "Breiman L. Random Forests (2001)", url: "https://link.springer.com/content/pdf/10.1023/A:1010933404324.pdf", kind: "PDF", note: "Оригінальна стаття: звідки походить сама ідея та оцінка out-of-bag." },
    { title: "MLU-Explain · Random Forest", url: "https://mlu-explain.github.io/random-forest/", kind: "демо", note: "Продовження: чому багато дерев разом стабільніші за одне." }
]' />

## Код: ліс і бустинг на даних «Титаніка»

```python
from sklearn.ensemble import GradientBoostingClassifier, RandomForestClassifier

rf = RandomForestClassifier(n_estimators=500, oob_score=True,
                            random_state=0, n_jobs=-1).fit(X_tr, y_tr)
gb = GradientBoostingClassifier(n_estimators=300, learning_rate=0.05,
                                max_depth=3, random_state=0).fit(X_tr, y_tr)

print(f"ліс:     OOB {rf.oob_score_:.3f}   тест {rf.score(X_te, y_te):.3f}")
print(f"бустинг: тест {gb.score(X_te, y_te):.3f}")
for name, imp in sorted(zip(X.columns, rf.feature_importances_), key=lambda t: -t[1]):
    print(f"  {name:6s} {imp:.3f}")
```

<RunOutput>

```text
ліс:     OOB 0.814   тест 0.828
бустинг: тест 0.843
  fare   0.305
  sex    0.264
  age    0.251
  pclass 0.091
  sibsp  0.050
  parch  0.039
```

</RunOutput>


Вивід — вісім рядків: `ліс: OOB 0.814 тест 0.828`, `бустинг: тест 0.843` і шість рядків важливості ознак: `fare 0.305`, `sex 0.264`, `age 0.251`, `pclass 0.091`, `sibsp 0.050`, `parch 0.039`.

Два спостереження. Оцінка поза мішком, 0,814, близька до чесної тестової, 0,828, — на цих даних вона працює як безкоштовна заміна перевірочної вибірки. І ліс тут не побив обрізане дерево з попередньої картки, яке дало 0,843: на маленькій таблиці з шістьма ознаками ансамблю просто немає де розгорнутися.

Джерело: [scikit-learn · Ensemble methods](https://scikit-learn.org/stable/modules/ensemble.html)

## Важливість ознак за зменшенням домішки

Ліс уміє сказати, які ознаки він використовував. Найдешевший спосіб — **важливість за зменшенням домішки** (*mean decrease in impurity*, MDI): для кожної ознаки додаємо приріст інформації всіх розбиттів, де вона брала участь, зважений на кількість зразків.


<Figure src="/figs/ext/scikit-learn.org-a9240a-sphx_glr_plot_permutation_importance_001.png" alt="random forest feature importances computed by mean decrease in impurity">

Джерело: [scikit-learn · Permutation Importance vs Random Forest Feature Importance](https://scikit-learn.org/stable/auto_examples/inspection/plot_permutation_importance.html)

</Figure>


Рисунок з документації бібліотеки показує ту саму задачу про «Титанік», але до таблиці навмисне додано дві безглузді ознаки: випадкове число `random_num` і випадкову категорію `random_cat`. За версією MDI випадкове число — третя за важливістю ознака в моделі.

Це не помилка реалізації, а властивість самої міри. Вона рахується на тренувальних даних і систематично завищує ознаки з великою кількістю різних значень: у неперервного шуму завжди знайдеться поріг, який випадково трохи покращить чистоту вузла.

## Пермутаційна важливість перевіряє ознаку на нових даних

Чесніша міра ставить пряме питання: наскільки погіршиться якість моделі на **відкладеній** вибірці, якщо значення однієї ознаки випадково перемішати між об'єктами. Зв'язок ознаки з міткою руйнується, решта даних лишається незайманою.


<Figure src="/figs/ext/scikit-learn.org-fc92b7-sphx_glr_plot_permutation_importance_002.png" alt="permutation importances of the same features measured on the held out set">

Джерело: [scikit-learn · Permutation importance on the test set](https://scikit-learn.org/stable/auto_examples/inspection/plot_permutation_importance.html)

</Figure>


Випадкове число, яке MDI поставила на третє місце, тут дає нуль — модель від його перемішування не втрачає нічого. Той самий ефект видно й на нашому власному запуску без штучних ознак: MDI поставила на перше місце вартість квитка з оцінкою 0,305. Перемішування на тесті дає інший порядок — стать коштує моделі 0,192 точності, вік 0,081, а вартість квитка лише 0,041.

Правило просте: MDI годиться для швидкого погляду, рішення ухвалюють за пермутаційною важливістю, порахованою на даних, яких модель не бачила.

## Корельовані ознаки ламають обидві міри

Є випадок, коли й пермутаційна важливість вводить в оману. Якщо дві ознаки майже дублюють одна одну, перемішування будь-якої з них модель переживає без втрат — потрібну інформацію вона просто дочитує з ознаки-двійника.


<Figure src="/figs/ext/scikit-learn.org-7fff0d-sphx_glr_plot_permutation_importance_multicollinear_002.png" alt="permutation importances collapse to zero when features are correlated">

Джерело: [scikit-learn · Permutation Importance with Multicollinear or Correlated Features](https://scikit-learn.org/stable/auto_examples/inspection/plot_permutation_importance_multicollinear.html)

</Figure>


На рисунку — ліс, навчений на Breast Cancer Wisconsin. Точність моделі на тесті становить близько 97 %, а пермутаційна важливість усіх тридцяти ознак не відрізняється від нуля: шкала осі не виходить за ±0,02. Модель явно щось використовує, але міра цього не бачить, бо радіус, периметр і площа ядра клітини несуть майже однакову інформацію.

Ліки описані в тій самій сторінці документації: згрупувати ознаки за кореляцією ієрархічною кластеризацією, лишити по одному представнику з групи й перерахувати важливості — після цього вони знову стають осмисленими.

<Materials :items='[
    { title: "scikit-learn · Permutation Importance with Multicollinear Features", url: "https://scikit-learn.org/stable/auto_examples/inspection/plot_permutation_importance_multicollinear.html", kind: "документація", note: "Готовий рецепт: кластеризація ознак за кореляцією перед вимірюванням важливості." },
    { title: "Molnar C. Interpretable Machine Learning", url: "https://christophm.github.io/interpretable-ml-book/feature-importance.html", kind: "книга", note: "Розділ про важливість ознак: що вона означає й чого не означає." }
]' />

## Бустинг: кожне наступне дерево вчиться на залишках

Беггінг будує дерева паралельно й незалежно. Бустинг заходить з іншого боку: дерева навчаються послідовно, і кожне наступне виправляє те, що не вдалося попереднім.

> *"Each tree is trying to predict residuals of the current prediction."*
> — UBC CPSC 340, лекція 23

У градієнтному бустингу прогноз — це не середнє, а **сума** дерев: $\hat{y}(\mathbf{x}) = \sum_{t=0}^{T} \nu f_t(\mathbf{x})$, де кожне дерево $f_t$ навчене на залишках $y_n - \hat{y}^{(t-1)}(\mathbf{x}_n)$, а множник $\nu$ — темп навчання.


<Figure src="/figs/own/lec06_06_16_boosting_stages.png" alt="gradient boosting after 1, 5 and 50 trees with residuals">
</Figure>


На рисунку — одновимірна регресія, дерева глибини 2, темп навчання 0,3. Після одного дерева синя крива — це дві сходинки, а червоні залишки майже повторюють дані: середньоквадратична похибка 0,379. Після п'яти дерев з'являється форма, похибка 0,138. Після п'ятдесяти сума сходинок відтворює криву, залишки сплющуються в шум, похибка 0,009.

Джерело методу: [Friedman J. Greedy Function Approximation, 2001](https://projecteuclid.org/euclid.aos/1013203451)

<Materials :items='[
    { title: "Friedman J. Greedy Function Approximation: A Gradient Boosting Machine (2001)", url: "https://projecteuclid.org/journals/annals-of-statistics/volume-29/issue-5/Greedy-function-approximation-A-gradient-boosting-machine/10.1214/aos/1013203451.full", kind: "стаття", note: "Стаття, з якої походить градієнтний бустинг." },
    { title: "Gradient Boosting explained — інтерактивно", url: "https://arogozhnikov.github.io/2016/06/24/gradient_boosting_explained.html", kind: "демо", note: "Дерева додаються по одному, і видно, як змінюється прогноз." }
]' />

## AdaBoost: ваги переходять на важкі приклади

Історично першим успішним бустингом був AdaBoost. Він теж будує пеньки один за одним, але замість залишків використовує ваги зразків: після кожного кроку неправильно класифіковані приклади важчають, і наступний пеньок змушений звернути на них увагу. Підсумковий прогноз — зважене голосування, де вагоміші ті пеньки, що помилялися рідше.


<Figure src="/figs/ext/scikit-learn.org-830fef-sphx_glr_plot_adaboost_twoclass_001.png" alt="adaboost decision boundary and distribution of decision scores for two classes">

Джерело: [scikit-learn · Two-class AdaBoost](https://scikit-learn.org/stable/auto_examples/ensemble/plot_adaboost_twoclass.html)

</Figure>


Ліворуч видно межу, яку багато простих порогових правил склали навколо вкладених хмар точок: жоден окремий пеньок так не вміє. Праворуч — розподіл підсумкових оцінок для двох класів; він показує те саме, що ймовірності в лекції 05: об'єкти в зоні перекриття отримують оцінку біля нуля, і саме на них модель невпевнена.

У бустингу беруть **слабкі моделі** (*weak learners*) — трохи кращі за випадкове вгадування. Це протилежність беггінгу: там потрібні глибокі дерева з малим зміщенням, тут — мілкі, бо зміщення виправить сама послідовність.

## Бустинг уміє перенавчатися, і це видно на графіку

У випадкового лісу зайві дерева нешкідливі: усереднення просто стає стабільнішим. У бустингу все навпаки — кожне нове дерево свідомо зменшує похибку на тренувальних даних, тому рано чи пізно починає підганяти шум.


<Figure src="/figs/ext/scikit-learn.org-5b307d-sphx_glr_plot_gradient_boosting_regression_001.png" alt="training and test deviance of gradient boosting versus number of iterations">

Джерело: [scikit-learn · Gradient Boosting regression](https://scikit-learn.org/stable/auto_examples/ensemble/plot_gradient_boosting_regression.html)

</Figure>


Синя крива тренувальної похибки спадає невпинно. Червона крива на відкладеній вибірці спадає лише перші півтори сотні ітерацій, після чого виходить на плато й далі не поліпшується. Дві криві розходяться — це та сама картина, що й у глибокого дерева, тільки замість глибини по горизонталі відкладено кількість дерев.

Звідси перше практичне правило бустингу: кількість дерев — це гіперпараметр, який треба зупиняти вчасно, а не «чим більше, тим краще».

## Три важелі регуляризації бустингу

Ранньої зупинки мало. Друге, що робить бустинг керованим, — це темп навчання $\nu$: кожне дерево додається не повністю, а помножене на невеликий множник, тож жодне окреме дерево не може різко змістити прогноз. Третій важіль — навчати кожне дерево не на всіх даних, а на випадковій частині.


<Figure src="/figs/ext/scikit-learn.org-1227ac-sphx_glr_plot_gradient_boosting_regularization_001.png" alt="test deviance of gradient boosting under shrinkage and subsampling">

Джерело: [scikit-learn · Gradient Boosting regularization](https://scikit-learn.org/stable/auto_examples/ensemble/plot_gradient_boosting_regularization.html)

</Figure>


Помаранчева крива без регуляризації досягає мінімуму й починає повзти вгору — класичне перенавчання. Малий темп навчання прибирає це зростання, але вимагає більше дерев: платимо часом за якість. Найнижче лежить сіра крива, де темп навчання й підвибірка застосовані разом.

Сучасні реалізації — [XGBoost](https://xgboost.readthedocs.io/), [LightGBM](https://lightgbm.readthedocs.io/) і [HistGradientBoosting](https://scikit-learn.org/stable/modules/ensemble.html#histogram-based-gradient-boosting) у scikit-learn — додають до цього штраф на ваги листків, гістограмний пошук порогів і вбудовану обробку пропусків.

## Беггінг і бустинг вирішують різні задачі

| Аспект | Беггінг і випадковий ліс | Бустинг |
|---|---|---|
| Порядок навчання | незалежно, паралельно | послідовно, кожне дерево залежить від попередніх |
| Що зменшує | дисперсію | зміщення |
| Які дерева | глибокі, з малим зміщенням | мілкі, слабкі моделі |
| Агрегація | середнє або голос більшості | зважена сума внесків |
| Реакція на зайві дерева | нешкідливі | перенавчання |
| Налаштування | працює майже без підбору | потребує темпу навчання й ранньої зупинки |

Практичний висновок такий. Випадковий ліс — розумний перший вибір: він дає добрий результат зі стандартними параметрами й майже не має способів зіпсувати результат. Бустинг за акуратного підбору майже завжди виграє в лісу, але вимагає часу на налаштування й уваги до кількості дерев.

## Порівняння методів на діагностиці пухлин

Візьмімо Breast Cancer Wisconsin, повторімо розбиття на тренувальну й тестову частини двадцять разів із різними випадковими станами й подивімось не лише на середнє, а й на розкид.


<Figure src="/figs/own/lec06_06_15_models_compared.png" alt="boxplot of test accuracy for knn tree bagging forest boosting">
</Figure>


Одне дерево дає в середньому 0,930 з розкидом 0,023 — найгірший результат і найширша коробка водночас. Беггінг піднімає середнє до 0,953, випадковий ліс — до 0,960 і водночас звужує розкид до 0,014, градієнтний бустинг — до 0,956.

Найцікавіше в цій картинці ліворуч: метод сусідів зі стандартизацією дає 0,965 і обходить усі ансамблі. Тридцять ознак цього набору справді описують геометрію клітинних ядер, тож близькі за ними пухлини схожі й за діагнозом — рівно те припущення, на якому метод побудований. Універсально найкращого алгоритму не існує, і це не фігура мови, а спостережуваний факт.

## Чотири межі рішення на одних даних

Наостанок подивімось на всі методи лекції разом. Дані — два переплетені півмісяці з великим рівнем шуму, 1200 точок; колір фону показує ймовірність класу, чорна лінія — межу рішення.


<Figure src="/figs/own/lec06_06_17_four_boundaries.png" alt="decision boundaries of knn tree forest and boosting on make moons">
</Figure>


Метод сусідів дає плавну межу, що повторює форму хмари, і 0,925 на тесті. Дерево глибини 3 малює три прямокутники — межа груба, але правило коротке; 0,903. Випадковий ліс складає сотні дерев у гладку межу з м'яким переходом ймовірності, 0,925. Градієнтний бустинг дає схожу картину з різкішими краями, 0,919.

Різниця в точності тут невелика, і це теж урок: на простій двовимірній задачі всі чотири методи справляються. Ансамблі відриваються там, де ознак десятки, а залежності між ними нелінійні.

## Обмеження методів цієї лекції

**Метод сусідів** нічого не рахує на етапі навчання, зате платить на кожному запиті: $O(ND)$ операцій і $O(ND)$ пам'яті на зберігання всієї вибірки. Він вимагає масштабування ознак, вдалої міри відстані та вироджується зі зростанням розмірності.

**Одне дерево** інтерпретовне, але нестабільне: перетасовка даних змінює правила. Жадібний пошук не гарантує оптимального дерева, а межа рішення завжди складається з відрізків, паралельних осям.

**Випадковий ліс** втрачає головну перевагу дерева — прочитати п'ятсот дерев неможливо, лишається тільки важливість ознак, яка сама потребує обережності при корельованих ознаках.

**Бустинг** чутливий до кількості дерев і темпу навчання, погано розпаралелюється й помітно реагує на шум у мітках, бо цілеспрямовано зменшує похибку саме на важких прикладах.

Спільна межа всіх чотирьох: вони працюють з ознаками, які їм дали. Будувати ознаки з сирих зображень або тексту вони не вміють — цим займуться нейронні мережі в лекціях 10–13.

## Підсумок лекції

**Сусіди й дерева не мають рівняння межі.** Перші зберігають дані й вирішують локально, другі розрізають простір послідовністю простих перевірок; обидва легко описують нелінійність.

**Параметр k і глибина дерева керують складністю.** В обох випадках працює той самий компроміс: занадто гнучка модель запам'ятовує вибірку, занадто груба нічого не бачить, і потрібну точку шукають на відкладених даних.

**Прокляття розмірності — головний ворог сусідів.** Зі зростанням кількості ознак околиця перестає бути локальною, і на Wine ми виміряли падіння з 0,966 до 0,704 після додавання двохсот шумових ознак.

**Ансамблі перетворюють нестабільність на перевагу.** Беггінг і випадковий ліс усереднюють дисперсію багатьох глибоких дерев, бустинг послідовно зменшує зміщення багатьох мілких.

**Важливість ознак треба читати обережно.** Міра за зменшенням домішки завищує ознаки з багатьма значеннями, а пермутаційна важливість занижує корельовані.

## Джерела й додаткові матеріали

**Курси**
- [EPFL CS-433](https://epfml.github.io/cs433-2025/) — лекція 06b: сусіди й розмірність
- [UBC CPSC 340](https://www.students.cs.ubc.ca/~cs-340/) — лекції 3, 6, 7, 23: дерева й ансамблі
- [Stanford CS231n](https://cs231n.github.io/classification/) — сусіди на зображеннях

**Оригінальні статті**
- [Breiman L. Random Forests, 2001](https://link.springer.com/article/10.1023/A:1010933404324)
- [Friedman J. Greedy Function Approximation, 2001](https://projecteuclid.org/euclid.aos/1013203451)

**Підручник**
- Hastie, Tibshirani, Friedman. [The Elements of Statistical Learning](https://hastie.su.domains/ElemStatLearn/) — розділи 9.2, 10, 15

**Документація scikit-learn**
- [Nearest Neighbors](https://scikit-learn.org/stable/modules/neighbors.html) · [Decision Trees](https://scikit-learn.org/stable/modules/tree.html)
- [Ensembles](https://scikit-learn.org/stable/modules/ensemble.html) · [Permutation importance](https://scikit-learn.org/stable/modules/permutation_importance.html)
